version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: ghcr.io/sushrut1101/docker:arch
        
jobs:
  my-job-name:

    executor: my-custom-executor
    steps:
      - checkout
      - run: |
         # git clone https://gitlab.com/Romdumprx/oneplus/cph2381.git 
         # git clone https://github.com/nobiraja/android_device_oneplus_CPH2381.git device/oneplus/CPH2381
          

workflows:
  my-custom-workflow:
    jobs:
      - my-job-name
